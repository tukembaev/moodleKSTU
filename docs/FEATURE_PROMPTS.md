# Промпты для внедрения фич (backend → frontend)

Backend-проект: `/Users/arif/Desktop/education2/apps` (Django + DRF).
Frontend-проект: `/Users/arif/Desktop/moodleKSTU` (React + Vite + TS, FSD, TanStack Query, shadcn/ui).

Порядок реализации: **4 → 3 → 5 → 6 → 9 → 1 → 8 → 7**.
Для каждого пункта — сначала BACKEND-промпт (в education2), потом FRONTEND-промпт (в moodleKSTU). Пункт 8 — только frontend.

---

## 4. Комментарий к оценке

### Backend промпт
В модели/эндпоинте, отвечающем за выставление оценки за сдачу (файл/тест), добавь опциональное текстовое поле "комментарий преподавателя" (nullable, необязательное при сохранении). Прокинь его через сериализатор на чтение и запись — как при выставлении оценки, так и при получении сдачи/списка сдач. Сделай миграцию. Никакой отдельной модели не нужно, это просто поле рядом с баллом. Убедись, что старые записи без комментария нормально отдаются (null/пустая строка).

### Frontend промпт
Добавь опциональное текстовое поле "Комментарий" в форму выставления оценки за сдачу (там же, где преподаватель ставит балл). Поле необязательное, показывается сразу под баллом. После сохранения комментарий должен отображаться там же, где показывается оценка — в просмотре сдачи студента и в списке сдач у преподавателя. Если комментария нет — просто не показывать блок.

---

## 3. Ручная оценка (без сдачи файла/теста)

### Backend промпт
Добавь возможность преподавателю выставить оценку студенту по теме курса без привязки к загруженному файлу или пройденному тесту (устный ответ, доска, бумажная контрольная). Нужна сущность "ручная оценка": курс, тема, студент, балл, опциональный комментарий (переиспользуй поле комментария из предыдущей фичи), кто и когда поставил. Эндпоинты: создать/обновить ручную оценку по студенту и теме, получить ручные оценки по теме (для препода) и по студенту (для агрегации в других фичах). Права: только преподаватель/сопреподаватель курса. Учти, что по одной паре (студент, тема) может быть и обычная сдача, и ручная оценка одновременно — не смешивай их в одну запись, они независимы.

### Frontend промпт
В интерфейсе темы курса для преподавателя добавь действие "Поставить оценку вручную" на уровне студента (в списке студентов курса/группы или рядом со сдачами по теме). По клику — модалка: балл + опциональный комментарий, сохранение через новый эндпоинт. Отображай ручные оценки в тех же местах, где обычные оценки за сдачу (журнал/список сдач), с пометкой, что оценка выставлена вручную (без файла).

---

## 5. Страница «Мои сдачи»

### Backend промпт
Сделай эндпоинт, который для текущего студента по конкретному курсу отдаёт агрегированный список: тема, дедлайн темы, статус (сдано/не сдано/просрочено), балл (из обычной сдачи ИЛИ из ручной оценки — что есть), комментарий преподавателя. Один элемент на тему курса, даже если по теме несколько попыток — бери последнюю/актуальную сдачу. Сортировка по порядку тем в курсе.

### Frontend промпт
Добавь внутри курса (студенческий вид) вкладку/страницу «Мои сдачи» — простая таблица: тема, срок сдачи, статус, балл, комментарий преподавателя. Без графиков и дашбордов, просто таблица со строками по темам курса. Клик по строке ведёт в соответствующую тему. Пустые/неактуальные значения (нет дедлайна, ещё не сдано) показывай прочерком или понятным статусом.

---

## 6. Все материалы курса в одном месте

### Backend промпт
Добавь эндпоинт, который отдаёт плоский список всех файлов-материалов по всем темам курса (без ответов студентов, только учебные материалы, прикреплённые к темам преподавателем): название файла, ссылка на скачивание, к какой теме относится, дата загрузки. Поддержи параметр поиска по названию файла (query-параметр, серверная фильтрация по подстроке, регистронезависимо).

### Frontend промпт
Добавь вкладку «Материалы» внутри курса: плоский список всех файлов курса (можно с группировкой по теме визуально), строка поиска сверху с фильтрацией по названию. Каждый файл — ссылка на скачивание/просмотр, показывай к какой теме он относится с переходом в тему.

---

## 9. Экспорт журнала оценок в Excel

### Backend промпт
Добавь эндпоинт экспорта ведомости курса в формате .xlsx: строки — студенты, столбцы — темы курса, ячейки — итоговый балл по теме (обычная сдача или ручная оценка — приоритет актуальному значению). Плюс итоговая колонка при наличии общей оценки за курс. Отдавай файл как attachment с понятным именем (название курса + дата). Права — только преподаватель/сопреподаватель курса.

### Frontend промпт
В существующей вкладке «Успеваемость» добавь кнопку «Экспортировать в Excel». По клику — запрос к новому эндпоинту, скачивание файла, во время запроса — состояние загрузки на кнопке, обработка ошибки тостом.

---

## 1. Объявления курса

### Backend промпт
Добавь сущность "объявление курса": курс, автор, текст, флаг "закреплено", дата создания. Эндпоинты: список объявлений курса (закреплённые сверху, затем по дате), создание/редактирование/удаление (только преподаватель/сопреподаватель). При создании объявления отправляй уведомление участникам курса через существующий websocket-механизм уведомлений (как для других событий курса). Права на чтение — все участники курса (включая ещё не открывавших темы).

### Frontend промпт
Добавь вкладку «Объявления» внутри курса — отдельная лента, независимая от обсуждений тем. Для преподавателя: форма создания объявления (текст + чекбокс "закрепить"), редактирование/удаление своих объявлений. Для всех участников: лента объявлений, закреплённые — сверху с визуальным выделением. Подключи получение новых объявлений через уже существующий websocket-канал уведомлений курса, чтобы лента обновлялась в реальном времени без перезагрузки.

---

## 10. Лента курса: объявления + неизменяемый журнал материалов

Цель: учебный процесс прозрачен. Если преподаватель загрузил лабораторную, студенты сделали работу, а потом файл заменили или удалили — это должно остаться в истории. Студент и администратор должны доказать, что материал менялся.

Вкладка на фронте уже называется «Лента курса». Она читает `GET course-feed/{courseId}/`. Пока эндпоинта нет, фронт собирает ленту из объявлений и текущего списка файлов (удаления/замены в этом режиме не видны).

### Backend промпт
Расширь объявления курса до ленты событий курса. Сохрани текущие CRUD объявлений (`course-announcements/{courseId}/`) без ломания контракта.

1. Добавь неизменяемый журнал учебных материалов (не ответы студентов). Модель события: id, course, author (кто сделал действие), kind (`material_created` | `material_updated` | `material_replaced` | `material_deleted`), дата создания, snapshot файла: `file_name`, опционально `previous_file_name` при замене, `theme` {id, title}, опционально ссылка на файл, если он ещё существует. **Записи журнала нельзя редактировать и удалять** ни преподавателю, ни API удаления материала — удаление файла только добавляет событие `material_deleted`, старая запись о загрузке остаётся.

2. Пиши события автоматически:
   - преподаватель загрузил файл в тему → `material_created`;
   - заменил тот же материал другим файлом → `material_replaced` (старое имя в `previous_file_name`, новое в `file_name`);
   - изменил метаданные файла без замены содержимого → `material_updated`;
   - удалил учебный материал → `material_deleted` с именем и темой на момент удаления.
   Не логируй сдачи студентов.

3. Эндпоинт `GET course-feed/{courseId}/` для всех участников курса. Ответ — плоский массив (или `{ items: [...] }`) смешанных записей, отсортированных по `created_at` desc. Закреплённые объявления (`is_pinned: true`) всегда сверху, затем хронология. Query-параметры: `sort=desc|asc` (по `created_at`), `kind=announcement|materials|all` (фильтр). Каждая запись:
   ```
   {
     "id": "uuid",
     "kind": "announcement" | "material_created" | "material_updated" | "material_replaced" | "material_deleted",
     "created_at": "ISO-8601",
     "updated_at": "ISO-8601 | null",
     "author": { "id", "full_name", "avatar", ... } | null,
     "is_pinned": false,
     "can_manage": false,
     "text": "только для announcement",
     "material": {
       "id": "id файла или null если уже удалён",
       "file_name": "lab.pdf",
       "previous_file_name": "old_lab.pdf | null",
       "file": "url или null",
       "theme": { "id": "...", "title": "Лабораторная 1" }
     }
   }
   ```
   Для объявлений `can_manage` как сейчас; для событий материалов всегда `can_manage: false`.

4. Права: читать ленту — участники курса. Писать объявления — преподаватель/сопреподаватель. Журнал материалов пишет только сервер при add/update/delete материала.

5. Уведомления: при создании объявления — как сейчас, type «Объявление курса», ссылка `/courses/{courseId}/feed/` (или `/announcements/` — фронт оба понимает). При add/replace/delete материала — уведомление участникам с type одним из: «Добавлен материал курса», «Изменён материал курса», «Удалён материал курса», тот же link на ленту. Текст: кто, какой файл, какая тема.

6. Миграция: старые объявления остаются в ленте как `kind=announcement`. Для уже существующих файлов один раз создай `material_created` по `uploaded_at` (или дате создания), без дублей при повторном деплое.

7. События журнала не удаляются каскадом вместе с файлом или темой: тема в snapshot остаётся текстом даже после удаления темы.

### Frontend промпт
Вкладка «Лента курса» читает `GET course-feed/{courseId}/` с `sort` и `kind`. Объявления по-прежнему через `course-announcements`. Клиентский fallback из файлов не используется.

---

## 8. Случайная выборка из банка вопросов (только frontend)

### Frontend промпт
Backend не трогать — итоговый тест по-прежнему создаётся с явным фиксированным списком ID вопросов, как сейчас. В диалоге выбора вопросов из банка добавь режим «Случайная выборка»: преподаватель выбирает банк/категорию и количество вопросов N, нажимает "Сгенерировать" — из отфильтрованного списка доступных вопросов на клиенте случайно выбираются N штук и подставляются в текущий выбор (как будто препод отметил их вручную). Дай возможность перегенерировать выборку до сохранения. Дальше форма сохранения теста работает как раньше, отправляя обычный список ID вопросов — никакой новой логики на сервере не появляется.

---

## 7. Новые типы вопросов (верно/неверно, короткий ответ, развёрнутый ответ)

### Backend промпт
Расширь модель вопроса банка/теста типом вопроса (enum), добавив к существующим single/multiple choice три новых типа: "верно/неверно" (одна булева правильная опция), "короткий ответ" (эталонный текст ответа, проверка на точное совпадение при сравнении, без учёта регистра и лишних пробелов), "развёрнутый ответ" (свободный текст, без автопроверки — требует ручной проверки преподавателем). Обнови сериализаторы вопросов и логику проверки ответов при прохождении теста: для верно/неверно и короткого ответа — автоматическая проверка как раньше, для развёрнутого — ответ сохраняется как есть и не засчитывается баллом автоматически, отметь его как "требует проверки", чтобы преподаватель мог поставить балл и комментарий вручную (используй уже существующий механизм ручной оценки/комментария). Сохрани полную обратную совместимость со старыми вопросами single/multiple choice — их поведение не меняется.

### Frontend промпт
В редакторе вопросов (банк вопросов и редактор теста) добавь выбор типа вопроса с тремя новыми вариантами и специфичной формой для каждого: верно/неверно — переключатель правильного ответа; короткий ответ — поле ввода эталонного текста; развёрнутый ответ — только текст вопроса, без указания правильного ответа. В интерфейсе прохождения теста студентом отрисовывай подходящий инпут под каждый тип (переключатель, текстовое поле, многострочное поле). В интерфейсе проверки ответов преподавателем добавь отображение ответов на развёрнутые вопросы как требующих ручной проверки, с возможностью поставить балл и комментарий через существующий механизм ручной оценки.

---

## 11. Нагрузка кафедры: аккордеон преподаватель → дисциплина → типы занятий

Фронт убирает вкладки «Каталог» / «Мониторинг». Одна страница:

1. карточки кафедр;
2. внутри кафедры — список преподавателей;
3. клик по преподавателю — accordion со списком его дисциплин;
4. клик по дисциплине — accordion с карточками типов занятий (как компактные карточки типов вопроса: иконка + короткое имя + число).

Число на карточке — **сумма текущих учебных файлов по всем темам этого типа**.  
Пример: две лабораторные темы, в первой 8 файлов, во второй 1 → карточка «Лабораторная» показывает **9**.  
Клиент не должен ходить по темам/файлам и суммировать сам.

### Backend промпт

Переделай API нагрузки кафедры так, чтобы фронт рисовал аккордеон без агрегации на клиенте и без эндпоинта `file-stats`.

**Что считается файлом.** Только текущие учебные материалы преподавателя, прикреплённые к темам курса. Не считать: ответы/сдачи студентов, удалённые файлы, тесты как попытки. Если у темы нет файлов — в сумму идёт 0.

**Канонические ключи `type_less`.** В ответе всегда стабильный ключ, даже если в БД лежит русская подпись (`"Лабораторная работа"`, `"Лб"`, `"УМК"`). Нормализуй в:

| type | short_label | label |
|------|-------------|-------|
| `lb` | Лб | Лабораторная работа |
| `pr` | Пр | Практическое занятие |
| `lc` | Лк | Лекционное занятие |
| `srs` | СРС | СРС |
| `rgz` | РГЗ | РГЗ |
| `rgr` | РГР | РГР |
| `umk` | УМК | УМК |
| `gl` | Глосс. | Глоссарий |
| `sb` | Силл. | Силлабус |
| `test` | Тест | Тест |
| `other` | Другое | Другое |

Неизвестное значение → `other`. Не отдавай в `type` русские строки.

**Эндпоинты.**

1. `GET departments/` — без ломания контракта. Как сейчас: `id`, `name`, `teachers_count`, `courses_count`, `files_count`. Список кафедр.

2. `GET departments/{organization_id}/teachers/` — без ломания контракта. Список преподавателей кафедры: `id`, `name`, `first_name`, `last_name`, `middle_name`, `position`, `avatar`, `courses_count`, `files_count`. Сортировка по ФИО. Это первый уровень после открытия кафедры. Курсы сюда **не вкладывай**.

3. `GET departments/{organization_id}/teachers/{teacher_id}/courses/` — **расширь**. Список дисциплин преподавателя на этой кафедре. Для каждого курса сразу отдай агрегацию по типам, чтобы раскрытие дисциплины на клиенте не делало второй запрос:

```json
{
  "id": "uuid курса",
  "discipline_name": "Базы данных",
  "category": "string | null",
  "files_count": 12,
  "by_type": [
    {
      "type": "lb",
      "short_label": "Лб",
      "label": "Лабораторная работа",
      "themes_count": 2,
      "files_count": 9
    },
    {
      "type": "umk",
      "short_label": "УМК",
      "label": "УМК",
      "themes_count": 1,
      "files_count": 3
    },
    {
      "type": "gl",
      "short_label": "Глосс.",
      "label": "Глоссарий",
      "themes_count": 1,
      "files_count": 0
    }
  ]
}
```

Правила `by_type`:
- одна запись на тип, у которого в курсе есть хотя бы одна тема;
- `files_count` = сумма файлов по всем темам этого типа (пример с двумя лабами выше);
- `themes_count` = число тем этого типа;
- тип без тем не отдавай; тип с темами, но без файлов — отдавай с `files_count: 0`;
- `files_count` курса = сумма `by_type[].files_count`;
- не клади в ответ список файлов, URL, темы по отдельности — только агрегаты;
- сортировка курсов по `discipline_name`; внутри `by_type` — в порядке таблицы ключей выше.

4. `GET departments/{organization_id}/file-stats/` фронт больше не использует. Можно оставить пока, можно удалить — на новый UI он не нужен.

**Права.** Как у текущих department-эндпоинтов (сотрудник). 401/403 без утечки чужих кафедр.

**Нагрузка.** Считай агрегаты на сервере (SQL `COUNT`/`GROUP BY` по теме → `type_less` → файлы материалов). Не отдавай сырой список тем/файлов, чтобы фронт не суммировал в цикле. Ленивая схема: курсы+`by_type` грузятся только при раскрытии преподавателя, не при открытии кафедры.

**Совместимость.** Поля `id`, `discipline_name`, `category`, `files_count` у курса оставь. `by_type` — новое обязательное поле этого списка. Старый формат `file-stats.by_type` (`type: "Лб"` без ключа) больше не используем.

### Frontend промпт

Убрать табы «Каталог» и «Мониторинг». Одна страница: кафедры → преподаватели → accordion дисциплин → accordion карточек `by_type` (иконка + `short_label`/`label` + `files_count`). Иконки мапятся на клиенте по `type`. `file-stats` не вызывать.
