# API Структура для системы замечаний (Remarks System)

## Описание системы

Система замечаний позволяет преподавателю оставлять замечания к работам студентов, а студентам отвечать на эти замечания. Преподаватель может одобрить или отклонить ответ студента.

## Типы данных

### ReviewMessageType (тип сообщения)
```typescript
type ReviewMessageType = 
  | "teacher_remark"       // Замечание от учителя
  | "student_reply"        // Ответ студента
  | "teacher_rejection"    // Отклонение учителя (с новым замечанием)
  | "teacher_approval";    // Одобрение учителя
```

### ReviewStatus (статус обсуждения)
```typescript
type ReviewStatus = 
  | "pending"              // Ожидает ответа студента
  | "student_replied"      // Студент ответил, ожидает реакции учителя
  | "approved"             // Учитель одобрил
  | "rejected";            // Учитель отклонил (цикл продолжается)
```

## Структура данных

### ReviewMessage (одно сообщение в треде)
```typescript
interface ReviewMessage {
  id: string;                          // Уникальный ID сообщения
  type: ReviewMessageType;             // Тип сообщения (см. выше)
  message: string;                     // Текст сообщения
  timestamp: string;                   // ISO 8601 формат: "2025-11-18T09:00:00Z"
  author_id: string;                   // ID автора
  author_name: string;                 // Имя автора (для отображения)
  author_role: "teacher" | "student";  // Роль автора
}
```

### Review (полное обсуждение/тред замечаний)
```typescript
interface Review {
  id: string;                    // Уникальный ID обсуждения
  theme_id: string;              // ID темы, к которой относится
  student_id: string;            // ID студента
  status: ReviewStatus;          // Текущий статус (см. выше)
  messages: ReviewMessage[];     // Массив всех сообщений в треде
  created_at: string;            // Дата создания (ISO 8601)
  updated_at: string;            // Дата последнего обновления (ISO 8601)
  
  // Ключи для UI логики:
  has_student_reply: boolean;    // true - студент ответил на замечание
  needs_teacher_action: boolean; // true - требуется действие учителя (одобрить/отклонить)
}
```

## Примеры данных

### Пример 1: Ожидает ответа студента
```json
{
  "id": "review_1",
  "theme_id": "theme_123",
  "student_id": "student_456",
  "status": "pending",
  "has_student_reply": false,
  "needs_teacher_action": false,
  "created_at": "2025-11-18T09:00:00Z",
  "updated_at": "2025-11-18T09:00:00Z",
  "messages": [
    {
      "id": "msg_1",
      "type": "teacher_remark",
      "message": "Вы не загрузили PDF файл с решением задачи №3",
      "timestamp": "2025-11-18T09:00:00Z",
      "author_id": "teacher_001",
      "author_name": "Иванов И.И.",
      "author_role": "teacher"
    }
  ]
}
```

### Пример 2: Студент ответил, ожидает проверки учителя
```json
{
  "id": "review_2",
  "theme_id": "theme_123",
  "student_id": "student_456",
  "status": "student_replied",
  "has_student_reply": true,
  "needs_teacher_action": true,
  "created_at": "2025-11-15T10:30:00Z",
  "updated_at": "2025-11-17T14:20:00Z",
  "messages": [
    {
      "id": "msg_1",
      "type": "teacher_remark",
      "message": "Вы не загрузили PDF файл с решением задачи №3",
      "timestamp": "2025-11-15T10:30:00Z",
      "author_id": "teacher_001",
      "author_name": "Иванов И.И.",
      "author_role": "teacher"
    },
    {
      "id": "msg_2",
      "type": "student_reply",
      "message": "Я загрузил PDF, извините, не заметил. Проверьте, пожалуйста",
      "timestamp": "2025-11-17T14:20:00Z",
      "author_id": "student_456",
      "author_name": "Петров П.П.",
      "author_role": "student"
    }
  ]
}
```

### Пример 3: Цикл отклонения
```json
{
  "id": "review_3",
  "theme_id": "theme_123",
  "student_id": "student_456",
  "status": "rejected",
  "has_student_reply": true,
  "needs_teacher_action": false,
  "created_at": "2025-11-14T09:00:00Z",
  "updated_at": "2025-11-18T11:00:00Z",
  "messages": [
    {
      "id": "msg_1",
      "type": "teacher_remark",
      "message": "В задаче №1 допущена ошибка в расчетах",
      "timestamp": "2025-11-14T09:00:00Z",
      "author_id": "teacher_001",
      "author_name": "Иванов И.И.",
      "author_role": "teacher"
    },
    {
      "id": "msg_2",
      "type": "student_reply",
      "message": "Исправил ошибку, пересчитал все значения",
      "timestamp": "2025-11-16T16:30:00Z",
      "author_id": "student_456",
      "author_name": "Петров П.П.",
      "author_role": "student"
    },
    {
      "id": "msg_3",
      "type": "teacher_rejection",
      "message": "Ошибка все еще присутствует. Проверьте формулу на странице 2",
      "timestamp": "2025-11-18T11:00:00Z",
      "author_id": "teacher_001",
      "author_name": "Иванов И.И.",
      "author_role": "teacher"
    }
  ]
}
```

### Пример 4: Одобрено
```json
{
  "id": "review_4",
  "theme_id": "theme_123",
  "student_id": "student_456",
  "status": "approved",
  "has_student_reply": true,
  "needs_teacher_action": false,
  "created_at": "2025-11-10T08:00:00Z",
  "updated_at": "2025-11-17T10:00:00Z",
  "messages": [
    {
      "id": "msg_1",
      "type": "teacher_remark",
      "message": "Отсутствуют выводы по работе",
      "timestamp": "2025-11-10T08:00:00Z",
      "author_id": "teacher_001",
      "author_name": "Иванов И.И.",
      "author_role": "teacher"
    },
    {
      "id": "msg_2",
      "type": "student_reply",
      "message": "Добавил раздел с выводами",
      "timestamp": "2025-11-16T12:00:00Z",
      "author_id": "student_456",
      "author_name": "Петров П.П.",
      "author_role": "student"
    },
    {
      "id": "msg_3",
      "type": "teacher_approval",
      "message": "Учитель одобрил",
      "timestamp": "2025-11-17T10:00:00Z",
      "author_id": "teacher_001",
      "author_name": "Иванов И.И.",
      "author_role": "teacher"
    }
  ]
}
```

## API Endpoints (предложение)

### Для преподавателя

#### 1. Получить все замечания для темы
```
GET /api/reviews/?theme_id={theme_id}
Response: Review[]
```

#### 2. Создать новое замечание
```
POST /api/reviews/
Body: {
  theme_id: string,
  student_id: string,
  message: string
}
Response: Review
```

#### 3. Одобрить ответ студента
```
POST /api/reviews/{review_id}/approve/
Response: Review (обновленный с новым сообщением типа "teacher_approval")
```

#### 4. Отклонить ответ студента
```
POST /api/reviews/{review_id}/reject/
Body: {
  message: string  // Причина отклонения
}
Response: Review (обновленный с новым сообщением типа "teacher_rejection")
```

### Для студента

#### 1. Получить свои замечания для темы
```
GET /api/reviews/my/?theme_id={theme_id}
Response: Review[]
```

#### 2. Ответить на замечание
```
POST /api/reviews/{review_id}/reply/
Body: {
  message: string
}
Response: Review (обновленный с новым сообщением типа "student_reply")
```

## Логика работы UI

### Для преподавателя:
1. Преподаватель видит кнопки "Одобрить"/"Отклонить" ТОЛЬКО когда:
   - `has_student_reply === true`
   - `needs_teacher_action === true`
   - `status === "student_replied"`

2. При нажатии "Одобрить":
   - Вызывается `POST /api/reviews/{review_id}/approve/`
   - Добавляется сообщение типа `teacher_approval` с текстом "Учитель одобрил"
   - `status` меняется на `"approved"`
   - `needs_teacher_action` становится `false`

3. При нажатии "Отклонить":
   - Преподаватель пишет комментарий
   - Вызывается `POST /api/reviews/{review_id}/reject/`
   - Добавляется сообщение типа `teacher_rejection`
   - `status` меняется на `"rejected"`
   - `needs_teacher_action` становится `false`
   - Цикл повторяется: студент должен снова ответить

### Для студента:
1. Студент видит кнопку "Ответить на замечание" когда:
   - `needs_student_action === true` (новое поле, которое можно добавить)
   - `status === "pending"` или `status === "rejected"`

2. При ответе:
   - Вызывается `POST /api/reviews/{review_id}/reply/`
   - Добавляется сообщение типа `student_reply`
   - `status` меняется на `"student_replied"`
   - `has_student_reply` становится `true`
   - `needs_teacher_action` становится `true`

## Дополнительные поля (опционально)

Можно добавить для улучшения UX:

```typescript
interface Review {
  // ... существующие поля
  
  // Дополнительные:
  is_new_for_student?: boolean;      // Новое для студента (не прочитано)
  is_new_for_teacher?: boolean;      // Новое для учителя (не прочитано)
  needs_student_action?: boolean;    // Требуется действие студента
  unread_count?: number;             // Количество непрочитанных сообщений
}
```

## Визуальное представление

UI отображает сообщения в виде треда (как в GitHub):
- **Желтый** цвет + иконка восклицания ⚠️ — замечания учителя
- **Синий** цвет + иконка сообщения 💬 — ответы студента
- **Зеленый** цвет + галочка ✓ — одобрение учителя
- Вертикальная линия соединяет сообщения в одном треде
- Роль автора показана бейджем (Преподаватель/Студент)

