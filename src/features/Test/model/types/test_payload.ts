export interface TestPayload {
  title: string,
  description: string,
  opening_date: Date,
  deadline: Date,
  course: string[],
  max_points: number,
  link_form:string,
  link_doc:string,
}